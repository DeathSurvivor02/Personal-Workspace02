#ifndef SET_H
#define SET_H

class set{
    private:
        vector <string> elements;

    public:
        set(void){elements.resize(0);}
        set(int n){elements.resize;elements.pushb_back(n);}

        int getNumberofElements() const{return elements.size();}
        int getElements(int index) const{
            if (index<=elements[index])
        }
        void add(const int);
        void remove( const int);
}

#endif

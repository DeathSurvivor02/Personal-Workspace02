# Define a function called myfunc that takes in a Boolean value (i.e., either True or False). If True, return (not print) "Hello", and if False, return "Goodbye".
def myfunc(option):
    if (option == True):
        return "Hello"
    else:
        return "Goodbye"

print (word = myfunc(True))
